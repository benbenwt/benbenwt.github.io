```
开发，构建，继承，部署
1开发环境打包为包含依赖或服务器的jar包，再上传到服务器部署。
2通过直接提交代码，或者从第三方仓库拉取代码。然后在服务器进行构建打包，进而部署。让上传的文件更小。在服务器安装maven，下载依赖库，再进行maven build即可。tomcat使用boot自带的即可。

```

官网文档

```
安装：https://www.jenkins.io/doc/book/installing/linux/，不要使用docker，没必要，直接安装很方便。
使用jenkins构建一个java应用：https://www.jenkins.io/doc/tutorials/build-a-java-app-with-maven/
```

```
将文件复制到虚拟机上。
```

